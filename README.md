# Eztronix POS — desktop releases

Public installer builds for the Eztronix POS Windows desktop app.

- **Installers** (`.exe`) and `latest.yml` are uploaded here automatically by GitHub Actions when a version tag is pushed.

Do not commit application source to this repository.
